import { PagePlaceholder } from "@/components/page-placeholder";

export default function CatalogPage() {
  return (
    <PagePlaceholder
      eyebrow="Catalog"
      title="The catalog is coming"
      description="Product listings are on the way. Check back soon to browse."
    />
  );
}
